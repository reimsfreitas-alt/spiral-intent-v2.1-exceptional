import {NextResponse} from 'next/server';import crypto from 'node:crypto';import {createGrant} from '@/core/authorization';import {requireKeys} from '@/core/crypto';import {Intent} from '@/core/types';
export async function POST(req:Request){try{const b=await req.json();const amount=Number(b.amount);if(!Number.isInteger(amount)||amount<=0||amount>10_000_000)return NextResponse.json({error:'INVALID_AMOUNT'},{status:400});if(b.currency!=='brl')return NextResponse.json({error:'ONLY_BRL_SUPPORTED'},{status:400});const intent:Intent={intent_id:`intent_${crypto.randomUUID()}`,created_at:new Date().toISOString(),authorized_effect:{system:'stripe',operation:'refund',amount,currency:'brl'}};const {privateKeyHex}=requireKeys();return NextResponse.json(createGrant(intent,privateKeyHex));}catch(e:any){return NextResponse.json({error:e.message},{status:503});}}
