import {NextResponse} from 'next/server';import {jcs,sha256,verifyEd25519} from '@/core/crypto';import {getActiveKey} from '@/core/ledger';
export async function POST(req:Request){try{const ecr=await req.json();const{signature,...payload}=ecr;const canonical=jcs(payload);const integrity=payload?.hashes?.payload_sha256===sha256(jcs({...payload,hashes:{...payload.hashes,payload_sha256:''}}));const authorizedHash=sha256(jcs(payload.authorized_effect))===payload?.hashes?.authorized_effect_sha256;const observedHash=sha256(jcs(payload.observed_effect))===payload?.hashes?.observed_effect_sha256;const key=await getActiveKey('receipt_authority');const signatureValid=!!key&&verifyEd25519(canonical,signature,key.public_key_hex);return NextResponse.json({valid:integrity&&signatureValid&&authorizedHash&&observedHash,checks:{integrity,signatureValid,authorizedHash,observedHash},verdict:payload?.verification?.verdict});}catch(e:any){return NextResponse.json({valid:false,error:e.message||'VERIFY_FAILED'},{status:400});}}
