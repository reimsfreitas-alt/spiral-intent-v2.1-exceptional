import './globals.css';
export const metadata={title:'Spiral Intent — Independent Execution Assurance',description:'Independent verification of externally observed effects against authorized effects.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
