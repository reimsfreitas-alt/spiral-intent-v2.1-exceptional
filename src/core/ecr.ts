import crypto from 'node:crypto';import {ECR,ECRPayload,Execution,AuthorizedEffect,ObservedEffect} from './types';import {jcs,sha256,signEd25519} from './crypto';import {verifyEffect} from './verifier';
export function generateECR(intentId:string,executionId:string,authorizationId:string,a:AuthorizedEffect,e:Execution,o:ObservedEffect,privateKeyHex:string,keyId='ed25519-receipt-primary'):ECR{const verification=verifyEffect(a,e,o);const payload:ECRPayload={spec_version:'ievp/v2',receipt_id:`ecr_${crypto.randomUUID()}`,issued_at:new Date().toISOString(),intent_id:intentId,execution_id:executionId,authorization_id:authorizationId,authorized_effect:a,execution:e,observed_effect:o,verification,issuer:{id:'did:spiral:receipt-authority-01',key_id:keyId},hashes:{authorized_effect_sha256:sha256(jcs(a)),observed_effect_sha256:sha256(jcs(o)),payload_sha256:''}};payload.hashes.payload_sha256=sha256(jcs(payload));return{...payload,signature:signEd25519(jcs(payload),privateKeyHex)};}
