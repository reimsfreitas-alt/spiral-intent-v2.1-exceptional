# Spiral Intent — clean deployment package

This package was extracted from `Spiral-Intent-ready Rev1.zip`.

Included:
- `index.html` — the Rev.5 final interface, promoted to the repository root.

Excluded intentionally:
- Orb / Orb2
- Backup
- Rev1 / Rev2 / Rev3 / Rev4
- API/backend/test files
- `.vercel` local metadata
- temporary and historical material

The final HTML is self-contained (no local JS/CSS/image assets are required).
It is therefore suitable for the current static Vercel deployment.

Repository target:
`reimsfreitas-alt/spiral-intent-exceptional`
